Drop your 5 portrait reels in here, named exactly:

  reel-1.mp4   (Home)
  reel-2.mp4   (About)
  reel-3.mp4   (Portfolio)
  reel-4.mp4   (Editorial / Runway)
  reel-5.mp4   (Contact)

Until a file is added, that page falls back to a placeholder poster
image automatically — nothing will break.

You can also drop real gallery/editorial photos in here and update
the `src` paths in src/data/media.js to point at them instead of the
picsum.photos placeholders.
